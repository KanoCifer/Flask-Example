// RSS 领域纯函数已迁移到 @readinglist/utils/domain/rss.ts —— 重新导出以保持兼容
export {
  exampleFeeds,
  truncateSummary,
  getFeedHost,
  getFeedProtocol,
  getSubscriptionTitle,
} from '@readinglist/utils';
export type { ExampleFeed } from '@readinglist/utils';
